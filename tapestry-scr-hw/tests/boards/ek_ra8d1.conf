# Board overlay for Renesas EK-RA8D1 (Cortex-M85)
# RA8D1 selects FPU automatically. Modest stack size is sufficient since
# cf21_mix tests allocate only a handful of structs on the stack.

CONFIG_ZTEST_STACK_SIZE=2048
CONFIG_MAIN_STACK_SIZE=2048
