# Board overlay for ESP-WROVER-KIT (ESP32 procpu)
# cf21_mix() uses basic arithmetic; FPU must be enabled for fabsf().

CONFIG_FPU=y
CONFIG_ZTEST_STACK_SIZE=2048
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_ESP32_USE_UNSUPPORTED_REVISION=y
