# Kconfig — Tapestry L3 Transport / Wire Protocol
#
# Source this from your application Kconfig with:
#   rsource "../tapestry-os/subsys/transport/Kconfig"

config TAPESTRY_WIRE_AUTH_ENABLED
	bool "Enable HMAC-SHA256 gossip frame authentication"
	depends on MBEDTLS
	default n
	help
	  When enabled each transmitted gossip frame is signed with a 4-byte
	  truncated HMAC-SHA256 tag (first 4 bytes of the full digest) appended
	  after the frame bytes.  Received frames whose tag does not match the
	  expected value are silently dropped and logged at WRN level.

	  All elements in a deployment must use the same key
	  (CONFIG_TAPESTRY_WIRE_AUTH_KEY) or gossip will not flow.

	  When disabled the wire format is unchanged: no tag bytes are appended
	  and no crypto dependency is introduced.

config TAPESTRY_WIRE_AUTH_KEY
	string "HMAC-SHA256 pre-shared deployment key"
	depends on TAPESTRY_WIRE_AUTH_ENABLED
	default "tapestry-insecure-default"
	help
	  ASCII string used as the HMAC-SHA256 key.  Change this from the
	  default before deploying hardware; all elements in a deployment must
	  share the same key string.

config TAPESTRY_AUTO_ID_WINDOW_MS
	int "Auto-ID nonce exchange window duration (ms)"
	default 4000
	help
	  Duration of the boot-time nonce exchange window used by the auto-ID
	  protocol.  All co-booting elements must agree on this value implicitly
	  (no explicit end-of-window signal is exchanged), so it must be the same
	  across every element in a deployment.

	  During this window each booting element advertises id=ELEMENT_ID_INVALID
	  with its hardware nonce.  After the window each element rank-orders the
	  collected nonces and assigns itself the corresponding unclaimed ID.

	  Increase if boot-time radio congestion or slow BLE stack bring-up causes
	  elements to miss peers' nonce beacons (symptom: duplicate IDs at startup).
	  Decrease on faster hardware where 4 s of idle startup is undesirable.

config TAPESTRY_TRANSCEIVER_SYSLINK
	bool "Syslink P2P transceiver (Crazyflie 2.1 BL nRF51 radio)"
	depends on UART_INTERRUPT_DRIVEN
	default n
	help
	  Drone-to-drone gossip over the nRF51822 co-processor's existing
	  SYSLINK_RADIO_P2P_BROADCAST (0x0A) channel.  No nRF51 reflash required.
	  The stock Bitcraze firmware (PLATFORM=cf21bl) ships this capability.

	  All three drones must use the same radio channel (TAPESTRY_P2P_CHANNEL,
	  default 80 = 2480 MHz).  The transceiver sends SYSLINK_RADIO_CHANNEL at
	  init to activate the nRF51 link and lock the channel simultaneously.

	  cf21bl_crtp_log.c (CRTP console) may be compiled alongside this
	  transceiver — both share cf21bl_syslink_tx_mutex (cf21bl_syslink_tx.c)
	  to serialize USART6 TX.  Simultaneous consoles from multiple drones
	  still share one nRF51 radio config; verify a channel/timing plan
	  before relying on 3 consoles at once.

config TAPESTRY_P2P_CHANNEL
	int "nRF51 ESB P2P radio channel (0-125, maps to 2400+N MHz)"
	depends on TAPESTRY_TRANSCEIVER_SYSLINK
	range 0 125
	default 80
	help
	  Radio channel shared by all drones for P2P broadcast gossip.
	  Default 80 = 2480 MHz (standard Crazyflie CRTP channel).
	  Change to avoid interference when a crazyradio2 host is also active.

config CF21BL_RADIO_ADDR_OVERRIDE
	bool "Set this element's CRTP console address at boot"
	depends on TAPESTRY_TRANSCEIVER_SYSLINK
	default n
	help
	  Send SYSLINK_RADIO_ADDRESS (0x05) to the nRF51 at init so this
	  element answers the host console on 0xE7E7E7E7xx instead of the
	  factory 0xE7E7E7E7E7.

	  Every Crazyflie ships on the same address.  With two on one address
	  both nRF51s auto-ACK every poll from the host console tool; the
	  colliding ACKs collapse the link and the overloaded nRF51 corrupts
	  the syslink UART, so P2P frames are dropped before the STM32 ever
	  sees them.  Measured 2026-08-24: ack rate ~85% with one drone,
	  10-70% erratic with two, and syslink ck_fail jumping 3 -> 377 the
	  moment a second drone powered on.  One address per element removes
	  it.

	  P2P gossip is unaffected either way — the nRF51 broadcasts on a
	  hardcoded 0xFFE7E7E7E7, independent of this.

	  NOT persisted: this is a runtime syslink message, not the EEPROM
	  config block, so a power-cycle into the bootloader always comes back
	  on the factory address and cfloader keeps working.

	  Enabling this costs the one-binary-for-every-drone property, so it
	  is intended for instrumented multi-drone sessions, not for shows.

config CF21BL_RADIO_ADDR_LSB
	int "Last byte of the CRTP console address (0xE7E7E7E7xx)"
	depends on CF21BL_RADIO_ADDR_OVERRIDE
	range 0 255
	default 231
	help
	  Decimal, because Kconfig ints are decimal: 231 = 0xE7 (factory),
	  232 = 0xE8, 233 = 0xE9, 234 = 0xEA.

	  Leave one element on 231 so the address you type most often stays
	  the familiar one, and give each additional element its own value:
	    west build ... -- -DCONFIG_CF21BL_RADIO_ADDR_OVERRIDE=y \
	                      -DCONFIG_CF21BL_RADIO_ADDR_LSB=232

config TAPESTRY_MESH_RELAY
	bool "Two-hop opportunistic relay forwarding (BATMAN-inspired mesh)"
	default n
	help
	  When enabled, elements that receive a gossip frame for peer X
	  re-advertise it (with hop_count decremented) if X's logical_clock is
	  newer than the last-relayed clock tracked for X.  This allows elements
	  out of direct radio range to receive peer updates via an intermediate
	  relay node.

	  hop_count starts at 2 for first-party frames and is decremented by
	  each relay; frames with hop_count == 0 are not re-advertised, capping
	  relay depth at two hops to prevent broadcast storms.  A per-relay
	  jitter of 0-50 ms is introduced before re-advertising to reduce
	  collision probability on dense networks.

	  Relay logic (ring buffer, last-relayed clock table, flush timer) is
	  compiled out when this option is disabled, preserving identical
	  single-hop broadcast behavior on memory-constrained targets.
