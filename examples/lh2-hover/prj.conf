CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_LOG_BUFFER_SIZE=4096
CONFIG_LOG_OUTPUT=y
CONFIG_LOG_BACKEND_SHOW_COLOR=n
CONFIG_PRINTK=y

CONFIG_FPU=y
CONFIG_FPU_SHARING=y

# Motor ESCs
CONFIG_PWM=y

# IMU (gyro + accel for attitude control)
CONFIG_SENSOR=y
CONFIG_I2C=y
CONFIG_BMI08X=y
CONFIG_BMI08X_GYRO_TRIGGER_OWN_THREAD=y
CONFIG_SENSOR_ASYNC_API=y

# Barometer — included to satisfy the DTS node; not used for control
CONFIG_BMP388=y

# Stabilizer: rate + angle loop; NO altitude hold (lighthouse Z used instead)
CONFIG_CF21BL_STABILIZER=y
CONFIG_CF21BL_ANGLE_MODE=y

# Lighthouse: full 3D position hold (X/Y via stabilizer, Z via main loop PID)
CONFIG_CF21BL_LIGHTHOUSE=y
CONFIG_CF21BL_LIGHTHOUSE_POS_HOLD=y
CONFIG_CF21BL_POS_MAX_M=3
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# ESC protocol: OneShot125 (validated on tether 2026-07-03; delete to fall back to RC PWM 400 Hz)
CONFIG_CF21BL_ESC_ONESHOT125=y
