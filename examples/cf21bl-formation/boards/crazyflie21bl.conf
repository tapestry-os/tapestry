# Console on USART3 (PC10 TX / PC11 RX, 115200 baud).
# USART6 is occupied by the syslink P2P transceiver.
# Read: minicom -D /dev/ttyUSB0 -b 115200
# Disable motors: west build ... -- -DCONFIG_PWM=n
CONFIG_LOG_OUTPUT=y
CONFIG_LOG_BACKEND_SHOW_COLOR=n
